import React from 'react';
import ReactDOM from 'react-dom';
import CreatedRoutes from './Router';


ReactDOM.render(
  <React.StrictMode>
    <CreatedRoutes />
  </React.StrictMode>,
  document.getElementById('root')
);

